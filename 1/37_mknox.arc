(def cont-frac (nums denoms n)
  (def int (i)
    (if (> _ n) 0
	(/ nums._ (+ denoms._ (int (+ 1 i))))))
  int.1)

(cont-frac [1.0] [1.0] 19)

; I'm not a huge fan of the syntax, but foo.x => (foo x)
; I do like the fact that [...] => (fn (_) ...)